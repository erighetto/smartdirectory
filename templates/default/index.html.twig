{% extends 'base.html.twig' %}
{% block title %}{{ title }}{% endblock %}
{% block body %}
    <div class="jumbotron">
        <div class="container">
            <div class="mb-5">
                <h1>{{ title }}</h1>
            </div>
            <ul class="list-group">
                {% for item in categories %}
                    <li class="list-group-item"><i class="far fa-folder-open"></i>
                        {% set slug = item.name ~ " " ~ item.cid %}
                        <a href="{{ path('smart_directory', {'lev1': slug|slugify}) }}">{{ item.name }}</a>
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
    <div class="container">
        <h2>Questa in origine era la mia raccolta di link utili</h2>
        <p>Quindi prima ancora che nascesse la necessità SEO mi ero fatto una raccolta di link utili in primis a me.</p>
        <p>Ora le directory non sono più molto importanti per i fattori di ranking, ma se sei interessato a far recensire il tuo sito contattaci.</p>
        <p><a href="m&#97;&#105;lto&#58;in&#102;&#111;&#64;c&#111;nsule%6Ez%61w%65b&#46;&#37;&#54;E%6&#53;&#116;">inf&#111;&#64;co&#110;su&#108;enzaweb&#46;n&#101;t</a></p>
        <p><a href="https://www.consulenzaweb.net" target="_blank">consulenzaweb.net</a></p>
    </div>
{% endblock %}
